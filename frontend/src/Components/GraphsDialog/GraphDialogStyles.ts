import { makeStyles } from '@mui/styles'

const useStyles = makeStyles({
  dashboardContainer: {
    position: 'relative',
    width: '1000px',
    height: '600px',
  },
  dashboard: {
    width: '900px', 
    height: '600px'
  },
})

export default useStyles
