import { makeStyles } from '@mui/styles'

const useStyles = makeStyles({
  mainContainer: {
    backgroundColor: 'rgb(221, 253, 255)', // Light grey background
    width: '100%', 
    height: '100vh'
  },
})

export default useStyles
